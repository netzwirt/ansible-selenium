---
# defaults file for selenium

selenium_install_dir: /opt
#selenium_version: 2.47.1
#selenium_version: 2.51.0
selenium_version: "3.1.0"

selenium_user: selenium
selenium_home_dir: "{{ selenium_install_dir }}/{{ selenium_user }}"

#selenium_chromedriver_version: "2.20"
selenium_chromedriver_version: "2.27"

selenium_chromedriver_dir: "{{selenium_home_dir}}/driver"
